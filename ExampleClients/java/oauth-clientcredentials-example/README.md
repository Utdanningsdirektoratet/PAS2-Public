# OAuth2 client credential flow Java sample #

Make sure you have Maven installed, if not, install it using Scoop:
```posh
scoop install maven
```
Then, compile and package the project running:
```posh
mvn package
```
Finally, run the sample:
```posh
mvn exec:java
```